# Local run example (.env)

# GitHub token (must have repo/issues permission)
GITHUB_API_TOKEN=PASTE_YOUR_GITHUB_PAT_HERE

# admin password for local auth
ADMIN_PASSWORD=q1w2e3r4

# jwt secret for signing admin cookie
JWT_SECRET=replace_with_a_long_random_string

# optional: port
PORT=3000
